<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit your chat</title>
</head>
<body>
    <h3>Edit your chat id: <%= chat._id %></h3>
    <p>This chat was sent form : <b><%= chat.from %> </b>to<b> <%= chat.to %></b></p>
    
    <form action="/chats/<%=chat.id%>?_method=PUT" method="post">
        <textarea name="message" id="" cols="30" rows="10"><%= chat.message %></textarea><br>
        <button>Edit</button>
    </form>
</body>
</html>